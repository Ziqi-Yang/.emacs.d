;;; init-key.el --- Key Mappings Here -*- lexical-binding: t -*-
;;; Commentary:
;; 
;;; Code:

;;; parinfer simplify the way we write lisp
;; (use-package parinfer-rust-mode
;; 	 :hook emacs-lisp-mode)


(provide 'l-general)
